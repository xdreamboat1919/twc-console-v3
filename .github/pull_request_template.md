## What changed

## Why

## Checks

- [ ] `npm run verify` passes locally
- [ ] Domain logic changes have tests that fail without the change
- [ ] No new globals, no cross-layer imports
- [ ] Session schema updated if a persisted shape changed

## Operational note

<!-- If this alters a decision rule (thresholds, gate, verdicts), state the
     old behaviour, the new behaviour, and what evidence supports the change. -->
